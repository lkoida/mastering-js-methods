/**
 * Find the first person whose last name is 'Beckett'.
 * to check if string includes some substring use includes()
 * @param {import('../../data').people} people
 * @returns {String}
 */
export const findPersonByLastName = (people) => {
};

/**
 * Find the first person whose first name is 'Ludwig'.
 * to check if string starting form some substring use startsWith()
 * @param {import('../../data').people} people
 * @returns {String}
 */
export const findPersonByFirstName = (people) => {
};

/**
 * Find the first person whose last name starts with 'Ben'.
 *
 * @param {import('../../data').people} people
 * @returns {String}
 */
export const findPersonByLastNameStartsWith = (people) => {
};

/**
 * Find the first person whose last name ends with 'man'.
 * @param {import('../../data').people} people
 * @returns {String}
 */
export const findPersonByLastNameEndsWith = (people) => {
};

/**
 * Find the first person whose last name contains 'son'.
 * @param {import('../../data').people} people
 * @returns {String}
 */
export const findPersonLastNameContains = (people) => {
};

/**
 * @see https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/find
 */
/**
 * find user who have the property `isActive` set to true
 * @param {import('../../data/users').users} users
 */
export const findActive = (users) => {
};

/**
 * find user who have in friends person named as variable
 * use find() and some() methods
 * @see https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/some
 *
 * @param {import('../../data/users').users} users
 * @param {String} fiendName
 */
export const findByUserFriendName = (users, fiendName) => {
};
